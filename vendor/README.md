# Release inputs

Published releases place the reviewed `omarchy-knowledge-setup.py` and exact
`omarchy_community_knowledge_tools-0.4.1-py3-none-any.whl` here. They may remain
ignored during development, but release maintainers explicitly stage and commit
both pinned files so a normal plugin clone includes them.
